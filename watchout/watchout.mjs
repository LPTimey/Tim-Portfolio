
import { initInfoSpin } from "./watchturn.mjs";

async function main() {
    // startButton.innerHTML = play_icon;
    // startButton.setAttribute("data-state", "play");
    // startButton.addEventListener("click", initHero);
    // startButton.style.visibility = "hidden";
    initInfoSpin();
}

main()
